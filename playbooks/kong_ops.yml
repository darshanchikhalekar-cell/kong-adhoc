- name: Kong Ops – Adhoc Operations
  hosts: all
  gather_facts: false
  become: false   # raw + sudo inside command

  tasks:

    # =========================
    # WHOAMI
    # =========================
    - name: Who am I
      raw: whoami
      register: whoami_out
      changed_when: false
      when: (kong_cmd | default('')) == "whoami"

    - name: Whoami output
      debug:
        msg: "Executed as user: {{ whoami_out.stdout | trim }}"
      when: (kong_cmd | default('')) == "whoami"


    # =========================
    # KONG VERSION
    # =========================
    - name: Get Kong version
      raw: kong version
      register: kong_version
      changed_when: false
      when: (kong_cmd | default('')) == "version"

    - name: Kong version output
      debug:
        msg: "Kong version: {{ kong_version.stdout | trim }}"
      when: (kong_cmd | default('')) == "version"


    # =========================
    # KONG STATUS
    # =========================
    - name: Get Kong service status
      raw: systemctl is-active kong
      register: kong_status
      changed_when: false
      when: (kong_cmd | default('')) == "status"

    - name: Kong status output
      debug:
        msg: "Kong service status: {{ kong_status.stdout | trim }}"
      when: (kong_cmd | default('')) == "status"


    # =========================
    # RESTART KONG
    # =========================
    - name: Restart Kong
      raw: sudo systemctl restart kong
      when: (kong_cmd | default('')) == "restart"

    - name: Restart confirmation
      debug:
        msg: "Kong service restarted successfully"
      when: (kong_cmd | default('')) == "restart"


    # =========================
    # SUMMARY (ONLY IF CMD PROVIDED)
    # =========================
    - name: Set summary stats
      set_stats:
        data:
          action: "{{ kong_cmd }}"
      when: kong_cmd is defined
